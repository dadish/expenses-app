import React from 'react';
import ExpenseIcon from 'material-ui/svg-icons/action/account-balance';

const Logo = () => (
  <ExpenseIcon
    style={{
      width: '40px',
      height: '40px',
    }}
    color={'#fff'}
  />
);

export default Logo;
